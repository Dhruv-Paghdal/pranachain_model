To test and run the project on your system follow the below steps:

1. Run the data cleaning code to generate the CSV files.
2. Run the model development code to train model and see the performance.
3. Run the model saving code to save the model.
4. Access the model through Django UI, and upload patient_report.txt file to see the output.
